import { createStore } from 'vuex';
import AuthModule from './module/authModule';

export default createStore({
    state: {},
    mutations: {},
    actions: {},
    modules: {
        AuthModule
    }
});
